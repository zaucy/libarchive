name: bazel

on:
  push:
    branches:
      - "**"

jobs:
  test-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: bazelisk test //...

  test-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bazelisk test //...

  test-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: bazelisk test //...
